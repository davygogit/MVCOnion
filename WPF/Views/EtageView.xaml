<UserControl x:Class="WPF.Views.EtageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance Type=viewmodels:EtageViewModel}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,10">
            <Grid>
                <TextBlock Text="🏢 Gestion des Étages" 
                          FontSize="24" 
                          FontWeight="Bold"/>
                
                <Button Content="🔄 Actualiser"
                       Command="{Binding LoadDataCommand}"
                       Style="{StaticResource PrimaryButtonStyle}"
                       HorizontalAlignment="Right"
                       Padding="15,8"/>
            </Grid>
        </Border>

        <!-- Loading Indicator -->
        <Border Grid.Row="1" 
               Background="#80000000"
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
               Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳ Chargement..." 
                          FontSize="24" 
                          Foreground="White"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Etages List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Etages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" 
                               Width="300" 
                               Margin="10">
                            <StackPanel>
                                <!-- Image -->
                                <Border Background="#ECF0F1"
                                       CornerRadius="5"
                                       Height="200"
                                       Margin="0,0,0,15">
                                    <Grid>
                                        <TextBlock Text="🏢" 
                                                  FontSize="64" 
                                                  Foreground="#BDC3C7"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                        <!-- TODO: Add Image control with binding to ImgPlanEtagePath -->
                                    </Grid>
                                </Border>

                                <!-- Info -->
                                <TextBlock Text="{Binding Nom}" 
                                          FontSize="20" 
                                          FontWeight="Bold"
                                          Margin="0,0,0,10"/>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                    <TextBlock Text="Niveau: " Foreground="Gray"/>
                                    <TextBlock Text="{Binding Niveau}" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                    <TextBlock Text="Salles: " Foreground="Gray"/>
                                    <TextBlock Text="{Binding Salles.Count}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
